---
sidebar_position: 2
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


# Android
Library that encapsulates remote storage operations to upload/download files

### Getting Started
<Tabs>
<TabItem value="example" label="Example" default>

Uploading a file:
```kotlin
HumaObjectStorageManager.getInstance().uploadFile(
            filepath, // file path
            uploadProgress, // MutableLiveData<Int>, will consume 0-100 progress percentage values
            { s3Object -> /*file uploaded successfully*/ },
            { error -> /*failed to upload the file*/ }
)
```
Getting file download url:
```kotlin
HumaObjectStorageManager.getInstance().getDownloadUrl(
            s3ObjectKey,
            s3ObjectBucket,
            { url -> /*download url is ready*/ },
            { error -> /*failed to get download url*/ }
)
```

</TabItem>
<TabItem value="installing" label="Installing">

1. Add the dependency in your local build.gradle file:
```kotlin
implementation("com.huma.sdk:object-storage:<latest-version>")
```
2. Initialize `HumaUtilsManager` and `HumaObjectStoreManager` in your Application class:
```kotlin
HumaUtilsManager.init(/*..*/)
HumaObjectStorageManager.init(/*..*/)
```

</TabItem>
<TabItem value="readme" label="Readme" default>
  
  Object Storage module encapsulates remote storage operations to upload/download files, corresponding functions are present in HumaObjectStorageManager class.

 ### Documentation
[API Reference](https://github.com/huma-engineering/huma-android-sdk/releases)

 ### Dependencies
- [Huma Utils](../Utilities/Android)

</TabItem>
<TabItem value="changelog" label="Changelog">

 **[0.1.29]**

  - [#9](https://github.com/huma-engineering/huma-android-sdk/pull/9) Object Storage module and its sample
  
</TabItem>
</Tabs>
